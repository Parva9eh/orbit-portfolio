# API base URL including /api path (no trailing slash after host except path)
# Dev default if unset: http://localhost:8000/api
# Prod default if unset: /api (same-origin proxy)
# VITE_API_URL=http://localhost:8000/api
