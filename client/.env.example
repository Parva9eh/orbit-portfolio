# API base URL including /api path (no trailing slash)
# Dev default if unset: http://localhost:8000/api
# Production (Render API): https://YOUR-SERVICE.onrender.com/api
# VITE_API_URL=http://localhost:8000/api

# Comms contact form (Web3Forms) — free key from https://web3forms.com
# Inbox only on their dashboard — never commit real keys.
# VITE_WEB3FORMS_ACCESS_KEY=
